--------------------------DIGITAL LOGIC SIMULATOR------------------------------

Globally set look and feel of gates, inputs and outputs.

Add ability to pan (zoom?) stage.

Save stage to cookie in browser.

Option to write gate names as a hint.

Clock phase not drawn properly when clock running.

Clock speed does not change until clock is restarted.

Need central place for input and output values to be stored. Then draw based on this data.

Draw stage needs to completely re-draw all elements
beginning with clearing current stage.


--------------------------TURING TARPIT SIMULATOR------------------------------

Need method to load/save code for later use.

Need stop button that works with endless loops. (Could pause program to allow button click?)

Step back button not fully implemented.

As code runs you cannot always see current instruction in code window.
